declare module 'remark-gfm' {
	// Minimal declaration to satisfy TypeScript when types are not installed.
	// If you later install proper types, remove this file.
	const plugin: any;
	export default plugin;
}
